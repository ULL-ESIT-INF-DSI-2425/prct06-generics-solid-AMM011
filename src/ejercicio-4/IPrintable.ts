/**
 * Interface IPrintable
 * This interface defines a contract for classes that can be printed.
 */
export interface IPrintable {
    /**
     * Prints the contents of the object.
     * @returns The contents of the object as a string.
     */
    print(): void;
}